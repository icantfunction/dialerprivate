version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Preparing static dialer deployment"
    build:
      commands:
        - echo "No build step required"
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - config.js
  cache:
    paths: []
